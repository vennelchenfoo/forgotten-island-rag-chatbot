# Forgotten Island — Bathala-Alam
# Production requirements for Streamlit Community Cloud deployment
#
# NOTE ON MEMORY: The production pipeline uses BAAI/bge-large-en-v1.5 (~1.3 GB)
# and bge-reranker-base (~500 MB). Streamlit Cloud free tier is 1 GB RAM.
# If you hit memory limits, upgrade to a paid Streamlit tier or switch to lighter
# models: EMBEDDING_MODEL_NAME = "sentence-transformers/all-MiniLM-L6-v2"

# === Web framework ===
streamlit>=1.35.0

# === LlamaIndex core + integrations ===
llama-index-core>=0.12.0
llama-index-embeddings-openai>=0.3.0
llama-index-embeddings-huggingface>=0.4.0
llama-index-llms-groq>=0.3.0
llama-index-llms-openai>=0.3.0
llama-index-readers-file>=0.4.0

# === Reranker (cross-encoder only — no local embedding model needed) ===
# cross-encoder/ms-marco-MiniLM-L-6-v2 requires sentence-transformers + torch.
# No HuggingFace embedding model is loaded; only the reranker uses these.
sentence-transformers>=2.7.0,<3.0.0

# === PyTorch (CPU build — Streamlit Cloud has no GPU) ===
# 2.1.x is stable with sentence-transformers 2.x; 2.2+ can trigger meta-tensor
# errors when loading cross-encoder models on CPU-only hosts.
torch>=2.0.0,<2.2.0

# === Utilities ===
python-dotenv>=1.0.0
